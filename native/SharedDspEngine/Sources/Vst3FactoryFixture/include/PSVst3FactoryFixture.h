#pragma once

// The fixture exports the mandatory macOS VST3 module symbols from its C++
// implementation. This header exists only to satisfy SwiftPM's C-family target
// layout; production code must not link against the fixture.
